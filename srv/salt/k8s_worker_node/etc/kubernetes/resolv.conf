{%- set id = grains['id'] -%}
{%- set cluster_name = pillar['hostnames'][id]['cluster'] -%}
{%- set cluster_dns_nameserver = pillar['clusters'][cluster_name]['cluster_dns_nameserver'] -%}
nameserver {{ cluster_dns_nameserver }}
search svc.cluster.local cluster.local local
options ndots:5
